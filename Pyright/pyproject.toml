[project]
name = "ot-benchmark-pyright"
version = "0.1.0"
description = "Benchmarking pyright's ability to type-check occurrence typing"
requires-python = ">=3.13.0"

[tool.pyright]
include = ["*.py"]
venvPath = "."
venv = ".venv"
reportUnusedImport = false
