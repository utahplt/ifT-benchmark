[project]
name = "ot-benchmark-pyrefly"
version = "0.1.0"
description = "Benchmarking pyrefly's ability to type-check occurrence typing"
requires-python = ">=3.13"
dependencies = [
    "pyrefly>=0.62.0",
]

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]

